anguage: python
python:
  - "2.7"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - py.test test.py # or py.test for Python versions 3.5 and below
after_script:
notifications:
  recipients:
    - gabriel_choptian@hotmail.com